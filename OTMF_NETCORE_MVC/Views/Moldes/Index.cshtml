@model IEnumerable<OTMF_NETCORE_MVC.Models.Molde>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreMolde)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreMolde)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdMolde">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdMolde">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdMolde">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
