@model IEnumerable<OTMF_NETCORE_MVC.Models.Estandar>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreEstandar)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreEstandar)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdEstandar">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdEstandar">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdEstandar">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
