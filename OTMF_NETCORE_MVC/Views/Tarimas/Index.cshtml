@model IEnumerable<OTMF_NETCORE_MVC.Models.Tarima>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreTarima)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreTarima)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTarima">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTarima">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTarima">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
