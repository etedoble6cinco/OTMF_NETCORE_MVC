@model IEnumerable<OTMF_NETCORE_MVC.Models.Caja>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreCaja)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LogoCaja)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EtiquetaDeCaja)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreCaja)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LogoCaja)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EtiquetaDeCaja)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCaja">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCaja">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCaja">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
